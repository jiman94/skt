server:
  port: 10001
---  
kafka:
  topic:
    product: product-event-topic
    order: order-event-topic    
    member: member-event-topic
---    
kafka:
  bootstrap:
    servers: localhost:9092,localhost:9093,localhost:9094
---
spring:
  sleuth:
    enabled: true
    sampler:
      probability: 1
    baggage:
      remote-fields: client
    sampler.rate: 1
  zipkin:
    enabled: true
    base-url: http://localhost:9411
    